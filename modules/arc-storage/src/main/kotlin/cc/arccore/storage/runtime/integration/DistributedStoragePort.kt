package cc.arccore.storage.runtime.integration

/**
 * Future placeholder port for distributed storage backends (Redis, Hazelcast, etc.).
 *
 * This interface is intentionally empty at this stage. It signals architectural
 * intent and provides a stable extension point for distributed cache/storage
 * providers without committing to a specific API surface yet.
 */
interface DistributedStoragePort
